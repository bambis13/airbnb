.similar-category__item.left.slick-sub
  = image_tag(home.listing_photos[0].image, class: "similar-home")
  .listing-s__text
    %p.listing-category.bold.category_color
      #{home.home_category_sub.name}
      %span（推奨度: #{similarity}）
    %p.listing-title.bold #{home.name}
    %p.listing-price ¥8,761 /泊・キャンセル無料
    %span.listing-star ★★★★★
    %span.listing-reviewsum.bold 122

